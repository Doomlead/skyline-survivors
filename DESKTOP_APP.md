# Running Last Bastion as a standalone desktop app

The project now supports desktop execution via Electron.

## Run

```bash
npm install
npm start
```

This opens a native desktop window and serves the game from an embedded local HTTP server.

## Optional: browser mode

```bash
npm run start:web
```

Then open `http://localhost:8080` in your browser.
